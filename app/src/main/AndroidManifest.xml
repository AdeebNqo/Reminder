<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adeebnqo.alarmapp">

    <original-package android:name="com.android.alarmclock" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".activity.SwitchRinger"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_label"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.analytics.globalConfigResource"
            android:resource="@xml/analytics_global_config" />

        <provider
            android:name="com.android.alarmclock.AlarmProvider"
            android:authorities="com.android.alarmclock" />

        <activity
            android:name="com.android.alarmclock.AlarmClock"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation"
            android:label="@string/app_label"/>
        <activity
            android:name=".activity.LandingActivityLoader"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation"
            android:label="@string/app_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.AdvertActivity"
            android:label="@string/title_activity_advert"
            android:launchMode="singleInstance"/>
        <activity android:name=".activity.CreateNewEventActivity" />
        <activity android:name=".activity.EventActivity" />
        <activity android:name=".activity.EventListActivity" />
        <activity android:name=".activity.NoEventsActivity" />
        <activity android:name=".activity.intro.Introduction" />
        <activity
            android:name="com.android.alarmclock.SettingsActivity"
            android:label="@string/settings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.android.alarmclock.SetAlarm"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation"
            android:label="@string/set_alarm" />
        <activity
            android:name="com.android.alarmclock.AlarmAlert"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@style/alarm_alert" />

        <!--
             This activity is basically the same as AlarmAlert but with a more
             generic theme. It also shows as full screen (with status bar) but
             with the wallpaper background.
        -->
        <activity
            android:name="com.android.alarmclock.AlarmAlertFullScreen"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Wallpaper.NoTitleBar" />
        <activity android:name="com.android.alarmclock.ClockPicker" />

        <receiver android:name="com.android.alarmclock.AlarmReceiver">
            <intent-filter>
                <action android:name="com.android.alarmclock.ALARM_ALERT" />
                <action android:name="alarm_killed" />
                <action android:name="cancel_snooze" />
            </intent-filter>
        </receiver>

        <!--
             This service receives the same intent as AlarmReceiver but it does
             not respond to the same broadcast. The AlarmReceiver will receive
             the alert broadcast and will start this service with the same
             intent. The service plays the alarm alert and vibrates the device.
             This allows the alert to continue playing even if another activity
             causes the AlarmAlert activity to pause.
        -->
        <service android:name="com.android.alarmclock.AlarmKlaxon">
            <intent-filter>
                <action android:name="com.android.alarmclock.ALARM_ALERT" />
            </intent-filter>
        </service>

        <receiver android:name="com.android.alarmclock.AlarmInitReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.android.alarmclock.AnalogAppWidgetProvider"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/analog_gadget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/analog_appwidget" />
        </receiver>

        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/settings"
            android:parentActivityName=".activity.LandingActivityLoader">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.adeebnqo.alarmapp.activity.LandingActivityLoader" />
        </activity>
    </application>

</manifest>
